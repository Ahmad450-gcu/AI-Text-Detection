export default function Footer() {
  return (
    <footer className="py-8 text-center text-[11px] leading-relaxed text-muted">
      VerifyAI gives a probabilistic estimate based on writing-style patterns.
      <br />
      It is not proof of authorship and can be wrong — use judgment alongside it.
    </footer>
  );
}
